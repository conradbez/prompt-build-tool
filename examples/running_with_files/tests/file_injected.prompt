{{ config(
    promptdata={"topic": "transformers"},
    promptfiles={"instructions": "demo_files/instructions.txt"}
) }}
Is the following text an article about {{ promptdata("topic") }}?

{{ ref('article') }}

Reply with only valid JSON: {"results": "pass"} or {"results": "fail"}.
